# ── Credentials & secrets — NEVER commit these ──────────────────
credentials.json
# Saved browser sessions. These are login cookies for the seller's own Facebook and eBay
# accounts — committing one hands over the account. They live in %LOCALAPPDATA%\ING AutoLister
# now (see AppPaths), but an old build could still have left a copy in the repo folder, and
# .bak/.tmp/.expired sit beside each of them.
facebook-session.json*
terapeak-session.json*
*.env
.env*
secrets.json
ingmining-server/ebay-config.php

# ── Build output ─────────────────────────────────────────────────
bin/
obj/
dist/

# ── Runtime data (generated at runtime, not source) ──────────────
App_Data/
photos/
wwwroot/generated-photos/
**/generated-photos/

# ── Logs ─────────────────────────────────────────────────────────
*.log
logs/

# ── OS & editor noise ────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.vs/
*.user
*.suo
.vscode/
*.swp

# ── Misc files to exclude ─────────────────────────────────────────
dashboard_verify.png
CUsers*.mjs
installer-out/
*.claude/
# Session screenshots and reference files
app_*.png
feature_overview.png
no_logo.png
ingmining_homepage_reference.html

# ── .NET ─────────────────────────────────────────────────────────
*.pdb
*.nupkg
publish/

# ── ingmining.com website data — kept in a separate folder, never in this repo ──
# (see C:\Users\nsquires\source\repos\ingmining-com-website\ — moved out of this repo
# entirely, this app has nothing to do with the ingmining.com website itself)
ingmining-server/
ingmining_homepage_*.html
docs/_elementor_elements.json
docs/_homepage_full_block.html
docs/homepage-listing-engine-banner.html
docs/wordpress-listing-engine-block.html
docs/inglisting-com-*.html
docs/listing-engine-demo.html
docs/assets/
build_pages.py
wordpress-plugin/
ING eBay AutoLister/ebay-callback-relay.php

firmware/photobox/build/
